@page
@model RazorPagesTutorial.Pages.Employee.IndexModel
@{
    ViewData["Title"] = "Index";
}

<h1>Employee</h1>
<div class="card-deck">
    @foreach( var employee in Model.employees)
    {
 var ppath = "/Users/noorisadeq/Projects/RazorPagesTutorial/RazorPagesTutorial/wwwroot/images/" + (employee.photoPath ?? "noimage.jpg");
        <div class="card m-3" style="min-width: 18rem; max-width:30.5%;">
            <div class="card-header">
                <h3>@employee.Name</h3>
            </div>
            <img class="card-img-top imageThumbnail" src="@employee.photoPath" asp-append-version="true" />

            <div class="card-footer text-center">
                <a asp-page="/Employee/Details" asp-route-ID="@employee.ID" class="btn btn-primary m-1">View</a>
                <a asp-page="/Employee/Edit" asp-route-ID="@employee.ID" class="btn btn-primary m-1">Edit</a>
                <a href="#" class="btn btn-danger m-1">Delete</a>
            </div> 
        </div>
    }
</div>
